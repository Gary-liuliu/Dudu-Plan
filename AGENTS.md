# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Cloud Build Artifacts

After an EAS cloud build, provide only the cloud install/download URL. Do not download APK or AAB artifacts into the workspace unless the user explicitly requests it.
